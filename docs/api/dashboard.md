---
title: "GET / (仪表盘)"
---

# GET / (仪表盘)

根路径提供内置的统计仪表盘，实时显示服务状态。不需要认证。

## 功能

- 服务状态徽章
- 容量条（活跃 / 最大并发上下文数）
- 请求统计
- 响应式布局

## 请求

在浏览器中打开，或：

```bash
curl http://localhost:7860/
```

返回包含仪表盘 UI 的 HTML 页面。
